import { CarbonOptions } from './faces';
export declare class Carbon {
    private options;
    constructor(options: CarbonOptions);
    generate(code: string, outputFilePath: string): Promise<string>;
    private download;
    private createParameter;
    private checkOptions;
    private checkLang;
    private checkTheme;
    private checkFont;
}
